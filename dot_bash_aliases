alias ls='\
	\ls \
	--color=auto \
	--classify=auto \
	--hyperlink=auto \
	--group-directories-first \
'

alias la='\
	\ls \
	--almost-all \
	--color=auto \
	--classify=auto \
	--hyperlink=auto \
	--group-directories-first \
'

alias ll='\
	\ls \
	-l \
	--all \
	--human-readable \
	--kibibytes \
	--color=auto \
	--classify=auto \
	--hyperlink=auto \
	--time-style=long-iso \
	--group-directories-first \
'

alias eza='\
	\eza \
	--color=auto \
	--icons=never \
	--classify \
	--hyperlink \
	--group-directories-first \
'

alias ls='eza'

alias la='\
	\eza \
	--all \
	--color=auto \
	--icons=never \
	--classify \
	--hyperlink \
	--group-directories-first \
'

alias ll='\
	\eza \
	--long \
	--header \
	--binary \
	--all --all \
	--color=auto \
	--color-scale \
	--icons=never \
	--time-style long-iso \
	--time modified \
	--git \
	--git-repos \
	--hyperlink \
	--octal-permissions \
	--classify \
	--smart-group \
	--group-directories-first \
'

alias bat='\
	\bat \
	--theme TwoDark \
	--tabs 4 \
'

alias cat='bat'

alias tv='tidy-viewer'

alias ccls='\
	cd && \
	clear && \
	ls && \
'

alias ccll='\
	cd && \
	clear && \
	ll && \
'

alias ..1='\cd ../'
alias ..2='\cd ../../'
alias ..3='\cd ../../../'
alias ..4='\cd ../../../../'
alias ..5='\cd ../../../../../'
alias ..6='\cd ../../../../../../'
alias ..7='\cd ../../../../../../../'
alias ..8='\cd ../../../../../../../../'
alias ..9='\cd ../../../../../../../../../'
alias ..='..1'
alias ...='..2'
alias ....='..3'
alias .....='..4'
alias ......='..5'
alias .......='..6'
alias ........='..7'
alias .........='..8'
alias ..........='..9'

alias sudo="\sudo "
alias mkdir='\mkdir -pv'
alias cg='cd `git rev-parse --show-toplevel`'

alias vim="nvim"
alias view="nvim -R"

alias grep='\grep --color=auto'
alias egrep='\egrep --color=auto'
alias fgrep='\fgrep --color=auto'

alias path='\echo -e ${PATH//:/\\n}'
alias df='\df --human-readable --print-type'
alias du='\du --human-readable --total'
alias free="\free --human --total"

alias ,date='\date +"\
Time Zone:%t%t%Z
Time:%t%t%t%l:%M:%S %p
Date:%t%t%t%A, %e %B, %+4Y
Datetime:%t%t%l:%M:%S %p, %A, %e %B, %+4Y
ISO 8601:%t%t%FT%T%z%t
Unix Epoch:%t%t%s
============================== Extra ==============================
Week of Year (00..53):%t%U (Sunday as First)
Week of Year (00..53):%t%W (Monday as First)
Day of Year (001.366):%t%j\
"'
alias now='\
echo "@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@ Current Locale ($(timedatectl show -p Timezone | cut -d= -f2)) @@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@";\
,date;\
echo;\
echo "@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@       UTC      @@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@";\
,date --utc'

# Safety Nets
alias rm='\rm --interactive=always --preserve-root'
alias mv='\mv --interactive'
alias cp='\cp --interactive'
alias ln='\ln --interactive'
alias wget='\wget --continue'

mkcdir() {
	mkdir -p $1
	cd $1
}
alias mkdircd="mkcdir"

# alias count-pdf-pages='pdfinfo {} | grep -oP '\''(?<=Pages:          )[ A-Za-z0-9]*'\'''

count-pdf-pages () {
	pdfinfo "$1" | \grep -oP '(?<=Pages:          )[ A-Za-z0-9]*'
}

alias ,gs='\git status'
alias ,ga='\git add'
alias ,gc='\git commit'
alias ,gp='\git pull'
alias ,gl='\git log'
alias ,glg='\git log --oneline --decorate --graph --all'
alias ,gb='\git branch'
alias ,gd='\git diff'

alias ,pyva='. .venv/bin/activate'
alias ,uvr='\uv run'
alias uvr=',uvr'
alias ,show-uv-tools='\uv tool list --show-paths --show-version-specifiers --show-with --show-extras --show-python'
,delta-py() {
	delta <(cat $1 | ruff check --fix --select I,F401 - | ruff format -) <(cat $2 | ruff check --fix --select I,F401 - | ruff format -)
}

alias ,rgu='rg --unrestricted --unrestricted'

# region:    Maqsam-specific
alias ,new-arc-diff='\arc diff --create --draft --nolint --nounit origin/master'
# enfregion: Maqsam-specific
